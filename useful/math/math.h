#pragma once

#include <cmath>

namespace math{
    float isZero(const float& number, const int& acc, const int& coef);

    float degToRad(const float& w);

    float radToDeg(const float& w);
}
